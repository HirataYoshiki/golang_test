version: '3'

services: 
  web: 
    image: go/test:1.0
    build: ./web
    container_name: go_app
    tty: true
    ports: 
      - 8000:8000
    stdin_open: true
    volumes: 
      - ../web:/home/user1/dev